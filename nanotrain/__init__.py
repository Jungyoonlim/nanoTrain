"""
nanotrain — reusable core behind the post-training demos.

    losses.py      pure loss functions (tensors in, scalar out, no model calls)
    logprobs.py    response-only sequence log-probability helpers
    tiny_models.py minimal GPT + reward model, zero HuggingFace dependency
"""
